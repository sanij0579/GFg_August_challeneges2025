Maximize the minimum difference between k elements
Solution:
class Solution:
    def maxMinDiff(self, arr, k):
        # Step 1: Sort the array
        arr.sort()
        
        # Step 2: Function to check if mid is a valid minimum difference
        def can_place(mid):
            count = 1   # pick the first element
            last = arr[0]
            
            for i in range(1, len(arr)):
                if arr[i] - last >= mid:
                    count += 1
                    last = arr[i]
                if count >= k:
                    return True
            return False
        
        # Step 3: Binary search on answer
        low, high = 0, arr[-1] - arr[0]
        ans = 0
        
        while low <= high:
            mid = (low + high) // 2
            
            if can_place(mid):
                ans = mid
                low = mid + 1  # try for a bigger minimum difference
            else:
                high = mid - 1 # try for a smaller minimum difference
        
        return ans
