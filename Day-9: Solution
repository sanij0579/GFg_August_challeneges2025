Longest Periodic Proper Prefix
Solution:
class Solution:
    def getLongestPrefix(self, s):
        n = len(s)
        if n <= 1:
            return -1

        # Build LPS array
        lps = [0] * n
        length = 0
        i = 1
        while i < n:
            if s[i] == s[length]:
                length += 1
                lps[i] = length
                i += 1
            else:
                if length != 0:
                    length = lps[length - 1]
                else:
                    lps[i] = 0
                    i += 1

        # Walk the border chain and find smallest positive border
        b = lps[-1]
        if b == 0:
            return -1

        min_border = b
        while b > 0:
            min_border = min(min_border, b)
            b = lps[b - 1]

        return n - min_border if min_border > 0 else -1
